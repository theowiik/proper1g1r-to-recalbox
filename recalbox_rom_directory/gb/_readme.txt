=======================================================================
# RECALBOX: System Game Boy (Handheld console)
=======================================================================

Copy your Game Boy ROMs here.
Supported extensions are: .bin .7z .gb .zip

This system can be emulated using 5 emulators.
These emulators are listed below, in order of compatibility/ease of use:
libretro-gambatte supports files of .gb .bin .zip .7z and is NOT Netplay compatible.
libretro-tgbdual supports files of .gb .bin .zip .7z and is NOT Netplay compatible.
libretro-mgba supports files of .gb .bin .zip .7z and is  Netplay compatible.
libretro-sameboy supports files of .gb .bin .zip .7z and is  Netplay compatible.
libretro-mesen_s supports files of .gb .zip .7z and is NOT Netplay compatible.

Device requirements:
- A controller is required
- A keyboard is unusable
- A mouse is unusable

This system allows to use compressed roms in .zip and or .7z.
But it is only an archive file. Files inside the .zip/.7z must match with extensions mentioned above.
Each .zip/.7z file must contain only one compressed rom.

More information and documentation at:
https://wiki.recalbox.com/en/emulators/handhelds/game-boy